#include <iostream>
#include <vector>

void process(const std::vector <int> &v1, std::vector <int> &v2) {
    std::vector <int> v_copy = v1;
    std::unique(v_copy.begin(), v_copy.end());
    std::sort(v_copy.begin(), v_copy.end());

    long long int v2.size();
    auto it_g = v2.begin();
    auto it_b = v_copy.begin();
    while(it_b - v_copy.begin() <= size)
    {   
        it_g
    }
}

